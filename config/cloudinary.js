module.exports = {
    clConfig: {
        cloud_name: process.env.CL_NAME,
        api_key: process.env.CL_APIKEY,
        api_secret: process.env.CL_SECRET,
        use_filename: true,
        folder: 'trailtalks/assets'
    },
		// other modules, helper functions, etc could also be exported from the module
}